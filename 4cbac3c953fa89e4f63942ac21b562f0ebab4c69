Close the last long-lived cache gaps: prime the real fs watcher before scanning (and drain it on close), prove startup delivery with a real filesystem test, rotate the active daily segment before the 256 MiB bound, and update the public auth/retention contract.
